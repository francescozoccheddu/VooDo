<Window
    x:Class="VooDo.Testbench.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xaml="using:VooDo.WinUI.Xaml" 
    xmlns:tb="using:VooDo.Testbench.Desktop"
    >

    <Grid
        Background="LightGray"
        >

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                x:Name="m_header"
                Grid.Row="0"
                Padding="10"
                Background="Gray">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock 
                    x:Name="m_headerText"
                    VerticalAlignment="Center">
                    Header
                </TextBlock>

            </Grid>

            <GridView 
                x:Name="m_gridView"
                Margin="10"
                Grid.Row="1"
                SelectionMode="None"
                HorizontalAlignment="Center"
                >

                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="tb:Item">
                        <Border 
                            Background="White"
                            Padding="10"
                            Width="150"
                            Height="200"
                            CornerRadius="4"
                            Margin="10"
                            Translation="0,0,20"
                            >
                            <Border.Shadow>
                                <ThemeShadow/>
                            </Border.Shadow>

                            <Grid>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="{x:Bind Title}"/>

                            </Grid>
                            
                        </Border>    
                    </DataTemplate>
                </GridView.ItemTemplate>
                
            </GridView>


        </Grid>

        <Border
            x:Name="m_buttonBorder"
            Margin="0,0,30,20"
            Padding="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Translation="{xaml:VooDo Code='new System.Numerics.Vector3(0,0,0)'}"
            Background="Black">

            <Button x:Name="m_button">
                Go up!
            </Button>

        </Border>

    </Grid>

</Window>
